/**
 * ? what is Node js?
 * * Node.js is a runtime environment that lets you run JavaScript outside the browser, mainly on a server.
 * 
 * ? what is V8 engine?
 * * * V8 is an open-source JavaScript engine developed by Google. It is written in C++ and is used in Google Chrome and Node.js to execute JavaScript code. V8 compiles JavaScript code into machine code, which allows for fast execution.
 * 
 * 
 * ?what is client side and server side?
 * 
 * * client side: refers to the part of a web application that runs in the user's browser. It includes everything that the user interacts with, such as the user interface, forms, and client-side logic. Client-side code is typically written in JavaScript and is responsible for handling user interactions, making API calls, and updating the UI dynamically.
 * 
 * * * server side: refers to the part of a web application that runs on the server. It includes the backend logic, database interactions, and server-side processing. Server-side code is typically written in languages like Node.js, Python, Ruby, or Java. It handles tasks such as processing requests from clients, managing data, and generating responses to be sent back to the client.
 * 
 * 
 * | Feature    | Client-Side          | Server-Side       |
| ---------- | -------------------- | ----------------- |
| Runs on    | Browser              | Server            |
| Focus      | UI / Interaction     | Logic / Data      |
| Tech       | HTML, CSS, JS, React | Node, Express, DB |
| Visibility | Visible to user      | Hidden from user  |

 * 
 *
 * ? Features of Node js
 * 1. Single-threaded: Node.js operates on a single thread, which allows it to handle multiple concurrent connections efficiently without blocking the execution of code.
 * 2. Event-driven: Node.js uses an event-driven architecture, which means that it responds to events (such as user actions or network requests) rather than waiting for tasks to complete before moving on to the next one.
 * 3. Asunchronous: Node.js uses asynchronous programming, which allows it to perform non-blocking operations. This means that while one task is being executed, other tasks can continue to run without waiting for the first task to finish.
 * 4. Cross-platform: Node.js can run on various operating systems, including Windows, macOS, and Linux, making it a versatile choice for developers.
 * 5. real-time applications: Node.js is well-suited for building real-time applications, such as chat applications or online gaming, due to its event-driven and non-blocking nature.
 * 6. non-blocking I/O: Node.js uses non-blocking I/O operations, which allows it to handle multiple requests simultaneously without waiting for each request to complete before moving on to the next one.
 *
 * 
 */ 